# QntyAgentEval — Codex entrypoint

Read `docs/AGENT_EVAL_V0_CONTRACT.md` before changing the harness.

This repository is evaluation infrastructure only. It has no authority over
Qnty/QntyLab scientific, protocol, economic, paper, shadow, or live state.

Use minimal diffs and keep task/scoring contracts deterministic. Never add an
LLM judge to a V0 hard gate.
