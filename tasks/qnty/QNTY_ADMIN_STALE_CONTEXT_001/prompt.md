You are working on a frozen historical Qnty checkout.

This is an ADMIN_LANE documentation task. Reconcile exactly one stale statement
in the root `CLAUDE.md` with the repository's actual development/CI
infrastructure.

Requirements:

- inspect the repository rather than trusting the stale statement;
- make the smallest docs-only correction;
- do not change protocol, scientific, runtime, continuity, receipt, or control
  state;
- do not create unrelated files;
- do not commit;
- do not use web search or external research; the repository is the evidence;
- run targeted verification appropriate for a docs-only change.

Finish with:

PLAN → CHANGESET → VERIFY → VERDICT

The exact final verdict must be `READY_FOR_REVIEW`.
